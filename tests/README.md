# Testing Strategy

- Unit tests live alongside source files within each package.
- Integration and contract tests are defined in this folder.
- Bun test runner is the default; add Testcontainers when external services are required.
