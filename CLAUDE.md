- for any comment started with // or /\* _/ \
  add _ ! ? for color rendering in the IDE by Better Comments extension
  // \* for information\
  // ! important matter\
  // ? for thing need to consider
- Always put private method after public method in every Class\
  sort the props and methods with the props and method name in ascending order
- Always put all the documentations MD generated to the docs folder
- Only generate documentation when it's important to know, don't simply generate any documentations for every given task
- Always create an index.ts to use barrel exports (export \* from './folder') in parent modules
- Always check if there is any TypeScript error show after every new implemented feature
- Always leave an empty line in end of the file